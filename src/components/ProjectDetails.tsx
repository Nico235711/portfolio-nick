
type ProjectDetailsProps = {
  url: string,
  label: string,
  githubUrl: string,
  techs: string[]
}

export const ProjectDetails = ({ url, label, githubUrl, techs }: ProjectDetailsProps) => {

  return (
    <div>ProjectDetails</div>
  )
}
